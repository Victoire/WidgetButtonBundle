{% extends 'VictoireCoreBundle:Widget:show.html.twig' %}

{% block content %}

{% set targetTag = target == "ajax-modal" ? 'data-toggle=' ~ target : 'target=' ~ target|default('_parent') %}
<a href="{{ vic_link_url(linkParameters) }}" {{ targetTag }} title="{{ hoverTitle }}" class="btn btn-{{ size }} btn-{{ style }}">
    {{ title|raw|nl2br }}
</a>
{% endblock content %}
