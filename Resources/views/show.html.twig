{% extends 'VictoireCoreBundle:Widget:show.html.twig' %}

{% block content %}
    {% spaceless %}
        {{ vic_link(link.parameters, label, attributes) }}
        {% block button_script %}
            {% if tooltipable %}
                <script>
                    <!--
                    $("#{{ tooltipId }}").tooltip();
                    -->
                </script>
            {% endif %}
        {% endblock button_script %}
    {% endspaceless %}
{% endblock content %}
