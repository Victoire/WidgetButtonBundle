{% block body %}

{#

#}
<div id="widget-{{slot}}" class="add-widget">
{% block form %}
    <form method="POST" action="{{path('victoire_cms_widget_create', {'page': page.id,'slot':slot, 'type':'button', 'entity': entity} )}}">
    {#Replace this default rendering by your own form#}
    {{form_row(form)}}
    {{form_rest(form)}}
{% block submit %}
    <input type="submit" value="{{ 'ok'|trans|desc('Ok') }}" />
    <a class="btn" class="cancel" onclick="cancelWidget(this);">{{ 'cancel'|trans|desc('Annuler') }}</a>
{% endblock submit %}
    </form>
{% endblock form %}
</div>
{% endblock body %}
